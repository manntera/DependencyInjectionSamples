/**
 * ログ出力の抽象化インターフェース
 * 
 * 【インターフェースを使う理由】
 * - 具体的な実装に依存せず、契約（インターフェース）に依存
 * - 複数の実装を簡単に切り替え可能
 * - テスタビリティの向上
 * - 将来の拡張性確保
 */
export interface ILogger {
  /**
   * メッセージをログとして出力
   * @param message 出力するメッセージ
   */
  log(message: string): void;
}